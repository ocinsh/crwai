package core

// Name is the product / binary name, used by the CLI and the MCP handshake.
const Name = "crwai"

// Version is the single source of truth for the product version. It is surfaced
// by `crwai version`, the `--version` flag, and the MCP server handshake.
const Version = "0.1.0"
