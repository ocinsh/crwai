package com.example.demo;

/** Café holds a unicode identifier to verify non-ASCII names parse. */
class Café {

    /** café returns a fixed greeting. */
    public String café() {
        return "expresso";
    }
}
