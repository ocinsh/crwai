// café returns a hot beverage emoji (function name uses unicode letters).
function café() {
  return "☕";
}

// Ωmega doubles its argument; both the name and the parameter are unicode.
const Ωmega = (λ) => λ * 2;

module.exports = { café, Ωmega };
