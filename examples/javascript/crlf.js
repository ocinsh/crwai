// greet returns a greeting (this file uses CRLF line endings).
function greet(name) {
  return "hi " + name;
}

// farewell returns a goodbye.
function farewell(name) {
  return "bye " + name;
}
