// café returns a warm greeting; the identifier carries an accent.
function café(): string {
  return "café ☕";
}

/** Ωmega doubles a value; the name is a Greek capital omega. */
const Ωmega = (λ: number): number => λ * 2;
